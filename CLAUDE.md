# Controle Financeiro

Monorepo com dois projetos:

- `API/` — backend Laravel 12 (PHP 8.5)
- `APP/` — frontend Vue

## Guidelines do Laravel Boost

As guidelines abaixo são geradas pelo `php artisan boost:install` em `API/CLAUDE.md`
e valem para o código dentro de `API/`. Regenere com `php artisan boost:update`
(rodando dentro de `API/`) — não edite o arquivo importado à mão.

@API/CLAUDE.md
